import React from 'react'
import { Link } from 'react-router-dom';

export const ActionButton = ({ path, btnName, customClass }) => {
  return (
    // <Link className={`border-2 font-oswald px-[25px] py-[10px] text-xl uppercase ${customClass}`} to={path}>{btnName}</Link>
    <div>
    </div>
  )
}
